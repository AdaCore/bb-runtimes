<?xml version="1.0" ?>

<gprconfig>
  <configuration>
   <config>
  package Compiler is
     -- Use -mfloat-gprs=single for e500v1
     -- Use -mfloat-gprs=double for e500v2
     Common_Required_Switches := ("-mfloat-gprs=double");

     for Leading_Required_Switches ("Ada") use
        Compiler'Leading_Required_Switches ("Ada") &amp;
        Common_Required_Switches;

     for Leading_Required_Switches ("C") use
        Compiler'Leading_Required_Switches ("C") &amp;
        Common_Required_Switches;
  end Compiler;

  package Linker is
    for Required_Switches use Linker'Required_Switches &amp;
      ("-L${RUNTIME_DIR(ada)}/adalib", "-lgnat",
      "-Wl,-u_start_ram",
      "-Wl,-T${RUNTIME_DIR(ada)}/arch/p2020.ld") &amp;
      Compiler.Common_Required_Switches;
  end Linker;

   package Binder is
      for Required_Switches ("Ada") use Binder'Required_Switches ("Ada")
        &amp; ("-nostdlib");
   end Binder;
   </config>
  </configuration>
</gprconfig>
